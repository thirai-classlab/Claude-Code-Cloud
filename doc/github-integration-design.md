# GitHub連携機能 設計書

| 項目 | 内容 |
|------|------|
| **ドキュメント名** | GitHub連携機能 設計書 |
| **バージョン** | 1.0 |
| **作成日** | 2026-01-01 |
| **ステータス** | Draft |

---

## 1. 概要

### 1.1 目的

Claude Code CloudプロジェクトとGitHubリポジトリを連携し、開発ワークフローをシームレスに統合する。

### 1.2 ゴール

| ゴール | 説明 |
|--------|------|
| **効率化** | GitHubリポジトリから直接プロジェクトを作成 |
| **統合** | Claude Code Cloud内でGit操作を完結 |
| **VSCode対応** | code-serverでも同じ認証設定で操作可能 |
| **AI支援** | コミットメッセージ・PR説明文のAI生成 |

### 1.3 システム構成図

```
┌─────────────────────────────────────────────────────────────────┐
│                    Claude Code Cloud                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐  │
│  │  Frontend   │  │   Backend   │  │     Workspace Volume    │  │
│  │  (Next.js)  │◄─►│  (FastAPI)  │◄─►│  /workspace/project-x/  │  │
│  └─────────────┘  └──────┬──────┘  │  ├── .git/               │  │
│                          │         │  ├── src/                │  │
│                          │         │  └── ...                 │  │
│                          │         └───────────┬──────────────┘  │
│                          │                     │                 │
│                          │         ┌───────────▼──────────────┐  │
│                          │         │     code-server          │  │
│                          │         │     (VSCode Web)         │  │
│                          │         └──────────────────────────┘  │
└──────────────────────────┼───────────────────────────────────────┘
                           │
                           ▼
              ┌────────────────────────┐
              │       GitHub API       │
              │  ├── OAuth認証         │
              │  ├── Repository API    │
              │  └── Pull Request API  │
              └────────────────────────┘
```

---

## 2. ユーザー利用シナリオ

### 2.1 シナリオ一覧

| # | シナリオ | 対象ユーザー | 優先度 |
|---|---------|-------------|:------:|
| 1 | GitHubリポジトリからプロジェクト作成 | 全ユーザー | 🔴 高 |
| 2 | コード編集後にCommit & Push | 全ユーザー | 🔴 高 |
| 3 | Pull Request作成 | 開発者 | 🔴 高 |
| 4 | ブランチ切り替え・作成 | 開発者 | 🟡 中 |
| 5 | Issueからタスク開始 | チーム開発 | 🟢 低 |

---

### 2.2 シナリオ詳細

#### シナリオ1: GitHubリポジトリからプロジェクト作成

**ペルソナ**: 田中さん（フリーランス開発者）

**状況**: 既存のGitHubリポジトリをClaude Code Cloudで編集したい

```
┌────────────────────────────────────────────────────────────────┐
│  ユーザーストーリー                                              │
├────────────────────────────────────────────────────────────────┤
│  As a 開発者                                                    │
│  I want to GitHubリポジトリからプロジェクトを作成したい          │
│  So that 既存のコードベースをClaudeと一緒に編集できる            │
└────────────────────────────────────────────────────────────────┘
```

**操作フロー**:

```
1. ホーム画面で「+ 新規プロジェクト」をクリック
                    │
                    ▼
2. 「GitHubからインポート」を選択
   ┌─────────────────────────────────────┐
   │  新規プロジェクト                    │
   │  ┌─────────────┐ ┌────────────────┐ │
   │  │ 空のプロジェ│ │ GitHubから     │ │
   │  │ クト        │ │ インポート ✓  │ │
   │  └─────────────┘ └────────────────┘ │
   └─────────────────────────────────────┘
                    │
                    ▼
3. GitHub連携（初回のみ）
   ┌─────────────────────────────────────┐
   │  GitHubアカウントを連携             │
   │                                      │
   │  [GitHubでログイン]                  │
   │                                      │
   │  または                              │
   │                                      │
   │  Personal Access Token:              │
   │  [________________________]          │
   │  [トークンで連携]                    │
   └─────────────────────────────────────┘
                    │
                    ▼
4. リポジトリを選択
   ┌─────────────────────────────────────┐
   │  リポジトリを選択                    │
   │  ┌─────────────────────────────────┐│
   │  │ 🔍 リポジトリを検索...          ││
   │  └─────────────────────────────────┘│
   │                                      │
   │  📁 my-awesome-app          ⭐ 125  │
   │     TypeScript • Updated 2h ago     │
   │                                      │
   │  📁 api-server              ⭐ 42   │
   │     Python • Updated 1d ago         │
   │                                      │
   │  📁 frontend-template       ⭐ 8    │
   │     React • Updated 3d ago          │
   └─────────────────────────────────────┘
                    │
                    ▼
5. ブランチを選択してプロジェクト作成
   ┌─────────────────────────────────────┐
   │  プロジェクト設定                    │
   │                                      │
   │  リポジトリ: user/my-awesome-app    │
   │  ブランチ:   [main ▼]               │
   │              ├── main               │
   │              ├── develop            │
   │              └── feature/new-ui     │
   │                                      │
   │  プロジェクト名:                     │
   │  [my-awesome-app____________]        │
   │                                      │
   │  [キャンセル]        [作成]          │
   └─────────────────────────────────────┘
                    │
                    ▼
6. Clone完了 → プロジェクトページへ遷移
```

**結果**: GitHubリポジトリがworkspaceにCloneされ、プロジェクトとして利用可能

---

#### シナリオ2: コード編集後にCommit & Push

**ペルソナ**: 佐藤さん（バックエンド開発者）

**状況**: Claudeと一緒にバグ修正を行い、GitHubにPushしたい

```
┌────────────────────────────────────────────────────────────────┐
│  ユーザーストーリー                                              │
├────────────────────────────────────────────────────────────────┤
│  As a 開発者                                                    │
│  I want to 編集したコードをCommit & Pushしたい                  │
│  So that チームメンバーと変更を共有できる                        │
└────────────────────────────────────────────────────────────────┘
```

**操作フロー**:

```
1. セッションでClaudeとコード編集
   ┌─────────────────────────────────────────────────────────────┐
   │ Chat                                          Git: main ▼  │
   ├─────────────────────────────────────────────────────────────┤
   │                                                             │
   │  👤 User                                                    │
   │  ログイン時のエラーハンドリングを改善してください            │
   │                                                             │
   │  🤖 Claude                                                  │
   │  auth.pyのエラーハンドリングを改善しました。                 │
   │  ┌─────────────────────────────────────────┐                │
   │  │ 📝 Modified: src/auth.py                │                │
   │  │ +15 lines, -3 lines                     │                │
   │  └─────────────────────────────────────────┘                │
   │                                                             │
   └─────────────────────────────────────────────────────────────┘
                    │
                    ▼
2. Gitパネルで変更を確認
   ┌─────────────────────────────────────┐
   │  Git Changes                    ✕   │
   ├─────────────────────────────────────┤
   │  Branch: main                       │
   │  ┌───────────────────────────────┐  │
   │  │ [Pull] [Push] [Sync]          │  │
   │  └───────────────────────────────┘  │
   │                                     │
   │  📝 Changes (2 files)               │
   │  ├── ✓ src/auth.py                 │
   │  └── ✓ tests/test_auth.py          │
   │                                     │
   │  ┌───────────────────────────────┐  │
   │  │ Commit Message:               │  │
   │  │ [________________________]    │  │
   │  │ [✨ AI生成]                   │  │
   │  └───────────────────────────────┘  │
   │                                     │
   │  [Commit] [Commit & Push]           │
   └─────────────────────────────────────┘
                    │
                    ▼
3. AI生成でコミットメッセージを作成
   ┌─────────────────────────────────────┐
   │  Commit Message:                    │
   │  ┌───────────────────────────────┐  │
   │  │ fix: improve error handling   │  │
   │  │ in authentication flow        │  │
   │  │                               │  │
   │  │ - Add specific error types    │  │
   │  │ - Include error codes in      │  │
   │  │   response                    │  │
   │  │ - Add unit tests              │  │
   │  └───────────────────────────────┘  │
   │           Generated by Claude ✨    │
   │                                     │
   │  [Commit & Push]                    │
   └─────────────────────────────────────┘
                    │
                    ▼
4. Push完了
   ┌─────────────────────────────────────┐
   │  ✅ Successfully pushed to origin   │
   │                                     │
   │  Commit: abc1234                    │
   │  Branch: main                       │
   │                                     │
   │  [View on GitHub] [Create PR]       │
   └─────────────────────────────────────┘
```

**VSCodeでの操作**:

```
code-serverでも同じ操作が可能:
  1. Source Controlパネルを開く (Ctrl+Shift+G)
  2. 変更ファイルをステージ
  3. コミットメッセージを入力
  4. Commit & Push

※ SSH認証設定が共有されているため、追加設定不要
```

---

#### シナリオ3: Pull Request作成

**ペルソナ**: 鈴木さん（フロントエンド開発者）

**状況**: 新機能を開発し、レビューのためにPRを作成したい

```
┌────────────────────────────────────────────────────────────────┐
│  ユーザーストーリー                                              │
├────────────────────────────────────────────────────────────────┤
│  As a 開発者                                                    │
│  I want to Pull Requestを作成したい                             │
│  So that チームにレビューを依頼できる                            │
└────────────────────────────────────────────────────────────────┘
```

**操作フロー**:

```
1. feature branchで開発完了後、PRボタンをクリック
   ┌─────────────────────────────────────────────────────────────┐
   │  Branch: feature/user-profile                     [Create PR]│
   └─────────────────────────────────────────────────────────────┘
                    │
                    ▼
2. PR作成ダイアログ
   ┌─────────────────────────────────────────────────────────────┐
   │  Create Pull Request                                    ✕   │
   ├─────────────────────────────────────────────────────────────┤
   │                                                             │
   │  feature/user-profile  →  main                              │
   │                                                             │
   │  Title:                                                     │
   │  ┌─────────────────────────────────────────────────────────┐│
   │  │ Add user profile page                                   ││
   │  └─────────────────────────────────────────────────────────┘│
   │                                                             │
   │  Description:                                   [✨ AI生成] │
   │  ┌─────────────────────────────────────────────────────────┐│
   │  │ ## Summary                                              ││
   │  │ - Add user profile page with avatar upload              ││
   │  │ - Implement profile edit functionality                  ││
   │  │ - Add form validation                                   ││
   │  │                                                         ││
   │  │ ## Changes                                              ││
   │  │ - `src/pages/profile.tsx` - New profile page            ││
   │  │ - `src/components/Avatar.tsx` - Avatar component        ││
   │  │ - `src/api/user.ts` - User API client                   ││
   │  │                                                         ││
   │  │ ## Testing                                              ││
   │  │ - [ ] Manual testing completed                          ││
   │  │ - [ ] Unit tests added                                  ││
   │  └─────────────────────────────────────────────────────────┘│
   │                                                             │
   │  Reviewers: [@team-lead, @senior-dev]                       │
   │  Labels: [enhancement] [frontend]                           │
   │                                                             │
   │  [Cancel]                              [Create Pull Request] │
   └─────────────────────────────────────────────────────────────┘
                    │
                    ▼
3. PR作成完了
   ┌─────────────────────────────────────────────────────────────┐
   │  ✅ Pull Request Created                                    │
   │                                                             │
   │  #42 Add user profile page                                  │
   │  feature/user-profile → main                                │
   │                                                             │
   │  [View on GitHub]                                           │
   └─────────────────────────────────────────────────────────────┘
```

---

#### シナリオ4: ブランチ切り替え・作成

**操作フロー**:

```
1. ヘッダーのブランチセレクターをクリック
   ┌─────────────────────────────────────────────────────────────┐
   │  Project: my-app                      Branch: [main ▼]      │
   └─────────────────────────────────────────────────────────────┘
                    │
                    ▼
2. ブランチを選択または新規作成
   ┌─────────────────────────────────────┐
   │  Switch Branch                      │
   ├─────────────────────────────────────┤
   │  🔍 Search branches...              │
   │                                     │
   │  ── Local ──                        │
   │  ○ main                             │
   │  ● develop (current)                │
   │  ○ feature/auth                     │
   │                                     │
   │  ── Remote ──                       │
   │  ○ origin/feature/new-api           │
   │                                     │
   │  ──────────────────────────         │
   │  + Create new branch                │
   └─────────────────────────────────────┘
                    │
                    ▼
3. 新規ブランチ作成
   ┌─────────────────────────────────────┐
   │  Create New Branch                  │
   ├─────────────────────────────────────┤
   │  Branch name:                       │
   │  [feature/___________________]      │
   │                                     │
   │  Base branch: [develop ▼]           │
   │                                     │
   │  [Cancel]        [Create & Switch]  │
   └─────────────────────────────────────┘
```

---

#### シナリオ5: Issueからタスク開始

**操作フロー**:

```
1. プロジェクトページのIssuesタブ
   ┌─────────────────────────────────────────────────────────────┐
   │  Sessions | Issues | Settings                               │
   ├─────────────────────────────────────────────────────────────┤
   │                                                             │
   │  Open Issues (3)                                            │
   │                                                             │
   │  ┌─────────────────────────────────────────────────────────┐│
   │  │ #15 🐛 Login fails on mobile devices                    ││
   │  │     Labels: [bug] [priority:high]                       ││
   │  │     Assignee: @me                                       ││
   │  │                                        [Start Session]  ││
   │  └─────────────────────────────────────────────────────────┘│
   │                                                             │
   │  ┌─────────────────────────────────────────────────────────┐│
   │  │ #14 ✨ Add dark mode support                            ││
   │  │     Labels: [enhancement]                               ││
   │  │                                        [Start Session]  ││
   │  └─────────────────────────────────────────────────────────┘│
   │                                                             │
   └─────────────────────────────────────────────────────────────┘
                    │
                    ▼
2. Issueを選択してセッション開始
   ┌─────────────────────────────────────────────────────────────┐
   │  Start Session from Issue                               ✕   │
   ├─────────────────────────────────────────────────────────────┤
   │                                                             │
   │  Issue #15: Login fails on mobile devices                   │
   │                                                             │
   │  Description:                                               │
   │  Users report that login button doesn't respond on          │
   │  iOS Safari. Console shows touch event error.               │
   │                                                             │
   │  ─────────────────────────────────────────────────          │
   │                                                             │
   │  ☑ Create new branch: fix/issue-15-mobile-login            │
   │  ☑ Include issue context in session                        │
   │                                                             │
   │  [Cancel]                              [Start Session]      │
   └─────────────────────────────────────────────────────────────┘
                    │
                    ▼
3. セッション開始（Issueコンテキスト付き）
   ┌─────────────────────────────────────────────────────────────┐
   │ Chat                                   Branch: fix/issue-15 │
   ├─────────────────────────────────────────────────────────────┤
   │                                                             │
   │  📋 Issue #15: Login fails on mobile devices                │
   │  ┌─────────────────────────────────────────────────────────┐│
   │  │ Users report that login button doesn't respond on iOS   ││
   │  │ Safari. Console shows touch event error.                ││
   │  └─────────────────────────────────────────────────────────┘│
   │                                                             │
   │  🤖 Claude                                                  │
   │  Issue #15 について確認しました。iOS Safariでのタッチイベント│
   │  の問題ですね。まず該当のログインコンポーネントを確認します。│
   │                                                             │
   └─────────────────────────────────────────────────────────────┘
```

---

## 3. 技術設計

### 3.1 認証方式

#### フォルダごとの認証切り替え（SSH方式）

```
~/.ssh/
├── config                      # SSH設定
├── id_ed25519_project_a        # Project A用秘密鍵
├── id_ed25519_project_a.pub    # Project A用公開鍵
├── id_ed25519_project_b        # Project B用秘密鍵
└── id_ed25519_project_b.pub    # Project B用公開鍵
```

**~/.ssh/config**:
```
# Project A (user-a)
Host github-project-a
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_project_a
    IdentitiesOnly yes

# Project B (user-b)
Host github-project-b
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_project_b
    IdentitiesOnly yes
```

**リポジトリ設定**:
```bash
# Project Aのworkspace
cd /workspace/project-a
git remote set-url origin git@github-project-a:user-a/repo.git

# Project Bのworkspace
cd /workspace/project-b
git remote set-url origin git@github-project-b:user-b/repo.git
```

#### メリット

| 項目 | 説明 |
|------|------|
| **VSCode対応** | code-serverでも同じ設定で動作 |
| **CLI対応** | Bash toolからのgit操作も可能 |
| **セキュリティ** | 秘密鍵は暗号化して保存 |
| **分離性** | プロジェクトごとに独立した認証 |

---

### 3.2 データモデル

```sql
-- GitHubアカウント（ユーザーごと）
CREATE TABLE github_accounts (
    id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    github_user_id VARCHAR(50) NOT NULL,
    github_username VARCHAR(100) NOT NULL,
    auth_type ENUM('oauth', 'pat', 'ssh') NOT NULL,
    encrypted_token TEXT,              -- OAuth/PAT用（暗号化）
    encrypted_private_key TEXT,        -- SSH用（暗号化）
    public_key TEXT,                   -- SSH公開鍵
    scopes JSON,                       -- 認可スコープ
    token_expires_at DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    UNIQUE KEY (user_id, github_user_id)
);

-- プロジェクトとリポジトリの連携
CREATE TABLE github_repo_links (
    id VARCHAR(36) PRIMARY KEY,
    project_id VARCHAR(36) NOT NULL,
    github_account_id VARCHAR(36) NOT NULL,
    repo_owner VARCHAR(100) NOT NULL,
    repo_name VARCHAR(100) NOT NULL,
    repo_full_name VARCHAR(200) NOT NULL,
    default_branch VARCHAR(100) DEFAULT 'main',
    current_branch VARCHAR(100),
    ssh_host_alias VARCHAR(100),        -- SSH config のHost名
    last_commit_sha VARCHAR(40),
    last_synced_at DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE,
    FOREIGN KEY (github_account_id) REFERENCES github_accounts(id) ON DELETE CASCADE,
    UNIQUE KEY (project_id)
);
```

---

### 3.3 API設計

#### 認証API

| エンドポイント | メソッド | 説明 |
|---------------|---------|------|
| `GET /api/github/auth/url` | GET | OAuth認証URL生成 |
| `POST /api/github/auth/callback` | POST | OAuthコールバック処理 |
| `POST /api/github/auth/pat` | POST | PAT登録 |
| `GET /api/github/accounts` | GET | 連携済みアカウント一覧 |
| `DELETE /api/github/accounts/{id}` | DELETE | 連携解除 |

#### リポジトリAPI

| エンドポイント | メソッド | 説明 |
|---------------|---------|------|
| `GET /api/github/repos` | GET | リポジトリ一覧 |
| `GET /api/github/repos/{owner}/{repo}` | GET | リポジトリ詳細 |
| `GET /api/github/repos/{owner}/{repo}/branches` | GET | ブランチ一覧 |
| `POST /api/projects/{id}/github/link` | POST | リポジトリ連携 |
| `DELETE /api/projects/{id}/github/link` | DELETE | 連携解除 |

#### Git操作API

| エンドポイント | メソッド | 説明 |
|---------------|---------|------|
| `GET /api/projects/{id}/git/status` | GET | Git status |
| `GET /api/projects/{id}/git/diff` | GET | 変更差分 |
| `POST /api/projects/{id}/git/commit` | POST | コミット |
| `POST /api/projects/{id}/git/push` | POST | プッシュ |
| `POST /api/projects/{id}/git/pull` | POST | プル |
| `GET /api/projects/{id}/git/branches` | GET | ブランチ一覧 |
| `POST /api/projects/{id}/git/branches` | POST | ブランチ作成 |
| `POST /api/projects/{id}/git/checkout` | POST | ブランチ切り替え |
| `POST /api/projects/{id}/git/commit-message` | POST | AIコミットメッセージ生成 |

#### Pull Request API

| エンドポイント | メソッド | 説明 |
|---------------|---------|------|
| `GET /api/projects/{id}/github/prs` | GET | PR一覧 |
| `POST /api/projects/{id}/github/prs` | POST | PR作成 |
| `GET /api/projects/{id}/github/prs/{number}` | GET | PR詳細 |
| `POST /api/projects/{id}/github/pr-description` | POST | AI PR説明文生成 |

#### Issue API

| エンドポイント | メソッド | 説明 |
|---------------|---------|------|
| `GET /api/projects/{id}/github/issues` | GET | Issue一覧 |
| `GET /api/projects/{id}/github/issues/{number}` | GET | Issue詳細 |

---

### 3.4 フロントエンド設計

#### 新規コンポーネント

| コンポーネント | パス | 説明 |
|---------------|------|------|
| `GitHubConnectButton` | `molecules/` | GitHub連携ボタン |
| `RepoSelector` | `organisms/` | リポジトリ選択モーダル |
| `BranchSelector` | `molecules/` | ブランチ選択ドロップダウン |
| `GitStatusPanel` | `organisms/` | Git状態パネル |
| `GitDiffViewer` | `organisms/` | 差分表示 |
| `CommitDialog` | `organisms/` | コミットダイアログ |
| `PullRequestDialog` | `organisms/` | PR作成ダイアログ |
| `IssueList` | `organisms/` | Issue一覧 |

#### Store設計

```typescript
// stores/githubStore.ts
interface GitHubStore {
  // State
  accounts: GitHubAccount[];
  currentRepoLink: GitHubRepoLink | null;
  gitStatus: GitStatus | null;
  branches: Branch[];
  pullRequests: PullRequest[];
  issues: Issue[];

  // Actions
  fetchAccounts: () => Promise<void>;
  connectOAuth: () => Promise<void>;
  connectPAT: (token: string) => Promise<void>;
  disconnectAccount: (id: string) => Promise<void>;

  fetchRepos: (accountId: string) => Promise<Repository[]>;
  linkRepo: (projectId: string, repo: Repository) => Promise<void>;
  unlinkRepo: (projectId: string) => Promise<void>;

  fetchGitStatus: (projectId: string) => Promise<void>;
  commit: (projectId: string, message: string) => Promise<void>;
  push: (projectId: string) => Promise<void>;
  pull: (projectId: string) => Promise<void>;

  fetchBranches: (projectId: string) => Promise<void>;
  createBranch: (projectId: string, name: string, base: string) => Promise<void>;
  checkoutBranch: (projectId: string, name: string) => Promise<void>;

  createPR: (projectId: string, data: CreatePRData) => Promise<void>;
  generateCommitMessage: (projectId: string) => Promise<string>;
  generatePRDescription: (projectId: string) => Promise<string>;
}
```

---

## 4. セキュリティ

### 4.1 セキュリティ要件

| 項目 | 対策 |
|------|------|
| **認証情報暗号化** | AES-256-GCMで暗号化、キーは環境変数 |
| **OAuth Scope** | `repo`, `read:user` 最小限 |
| **SSH鍵管理** | Ed25519鍵、パスフレーズ付き |
| **CSRF対策** | stateパラメータ検証 |
| **Rate Limit** | GitHub API制限対応、キャッシュ実装 |
| **監査ログ** | Git操作のログ記録 |

### 4.2 暗号化フロー

```
1. ユーザーがGitHub連携
           │
           ▼
2. OAuth Token または SSH秘密鍵を取得
           │
           ▼
3. AES-256-GCMで暗号化
   Key: GITHUB_ENCRYPTION_KEY (環境変数)
           │
           ▼
4. 暗号化データをDBに保存
           │
           ▼
5. 使用時に復号化してメモリ上で処理
           │
           ▼
6. 処理完了後、メモリから消去
```

---

## 5. 実装フェーズ

### 5.1 フェーズ計画

| フェーズ | 内容 | 期間 | 優先度 |
|---------|------|------|:------:|
| **Phase 1** | GitHub OAuth + アカウント管理 | 5日 | 🔴 |
| **Phase 2** | リポジトリ一覧 + Clone | 5日 | 🔴 |
| **Phase 3** | Git操作 (status/diff/commit/push) | 5日 | 🔴 |
| **Phase 4** | ブランチ管理 | 3日 | 🟡 |
| **Phase 5** | PR作成 + AI生成 | 5日 | 🟡 |
| **Phase 6** | Issue連携 | 3日 | 🟢 |
| **Phase 7** | UI改善 + テスト | 4日 | 🟡 |

### 5.2 マイルストーン

```
Week 1-2: Phase 1-2 (認証 + リポジトリ)
  ├── GitHub OAuth実装
  ├── アカウント管理UI
  ├── リポジトリ一覧
  └── Clone機能

Week 3-4: Phase 3-4 (Git操作)
  ├── status/diff表示
  ├── commit/push
  ├── pull
  └── ブランチ管理

Week 5-6: Phase 5-7 (PR + Issue + 仕上げ)
  ├── PR作成
  ├── AI生成機能
  ├── Issue連携
  └── テスト・改善
```

---

## 6. 技術選定

| 領域 | 技術 | 理由 |
|------|------|------|
| **GitHub API** | PyGitHub / httpx | 信頼性の高いPython GitHub クライアント |
| **Git操作** | GitPython | Python からのローカルgit操作 |
| **OAuth** | authlib | OAuth 2.0 実装 |
| **SSH鍵生成** | cryptography | Ed25519鍵ペア生成 |
| **暗号化** | cryptography (Fernet/AES-GCM) | トークン・秘密鍵暗号化 |
| **Diff表示** | react-diff-viewer-continued | 視覚的な差分表示 |

---

## 7. 参考資料

| 資料 | URL |
|------|-----|
| GitHub REST API | https://docs.github.com/en/rest |
| GitHub OAuth Apps | https://docs.github.com/en/apps/oauth-apps |
| GitPython | https://gitpython.readthedocs.io/ |
| PyGitHub | https://pygithub.readthedocs.io/ |

---

## 変更履歴

| 日付 | バージョン | 変更内容 | 担当 |
|------|-----------|---------|------|
| 2026-01-01 | 1.0 | 初版作成 | Claude |
